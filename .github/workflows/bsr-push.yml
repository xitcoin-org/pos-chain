name: Push to Buf Schema Registry
#  This workflow publishes Protobuf files to the Buf Schema Registry at
# https://buf.build/cosmos/evm when proto files change on main or a version tag
# is pushed.
on:
  push:
    branches:
      - main
    paths:
      - "proto/**"
      - "buf.work.yaml"
      - "buf.gen.yaml"
      - "buf.gen.proto.yaml"
    tags:
      - "v*.*.*"
permissions: read-all

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: bufbuild/buf-setup-action@v1.50.0
      # Push Cosmos EVM protos to the Buf Schema Registry
      - uses: bufbuild/buf-push-action@v1.2.0
        with:
          input: ./proto
          buf_token: ${{ secrets.BUF_TOKEN }}
