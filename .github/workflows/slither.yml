name: Slither Analysis

on:
  pull_request:
    branches: [main]
    paths:
      - 'contracts/**/*.sol'
      - '.github/workflows/slither.yml'
  workflow_dispatch:

permissions: read-all

jobs:
  analyze:
    name: Run Slither
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          submodules: recursive
          persist-credentials: false

      - name: Install contract dependencies
        run: |
          cd contracts
          npm ci
          cp -r node_modules/@openzeppelin .
          # Hardhat 3 prefixes build-info sources with `project/`. Slither's
          # resolver expects that virtual prefix to exist below the project.
          ln -s . project

      - name: Run Slither Action
        id: slither
        uses: crytic/slither-action@b52cc1cbfee9ca3e8722dd5224299d16c9a6b80f # v0.4.2
        with:
          # Point Crytic at the Hardhat project directory. Passing the config
          # file itself makes Crytic treat JavaScript as a Solidity source.
          target: contracts
          solc-version: 0.8.20
          node-version: 24
          # Dependencies and deliberately adversarial test fixtures/harnesses
          # are still compiled so imports resolve, but their findings are not
          # attributed to production code. Medium/high findings everywhere
          # else keep failing the job.
          slither-args: >-
            --exclude-dependencies
            --filter-paths "(^|/)(node_modules|@openzeppelin|testdata|tests|testutil)(/|$)|/(ContractCreationTester|ERC20RecursiveNonRevertingPrecompileCall|ERC20RecursiveRevertingPrecompileCall|ERC20WithNativeTransfers|ICS20TransferTester|SequentialICS20Sender|SequentialOperationsTester)\.sol$"
          fail-on: medium
